{
  "root": true,
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-console": ["warn", { "allow": ["warn", "error"] }]
  },
  "overrides": [
    {
      "files": ["scripts/**/*.ts", "db/seed.ts"],
      "rules": {
        "no-console": "off"
      }
    }
  ]
}
