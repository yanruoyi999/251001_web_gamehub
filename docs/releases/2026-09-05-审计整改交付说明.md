# Luma 审计整改交付说明

日期：2026-09-05，Asia/Shanghai。原始基线 main@bd0abb3。工作分支 fix/luma-audit-20260905。

## 实现范围

- S01：Next.js / eslint-config-next 15.5.25、sharp 0.35.4、实际 pnpm 锁文件；已核实 libheif 1.23.2。官方依据：https://nextjs.org/blog/august-2026-security-release 和 https://github.com/vercel/next.js/releases/tag/v15.5.25 。没有对生产环境做漏洞利用测试。
- B01/B04/B06/O01：纸牌完成状态、冻结计时、撤销及重玩、Draw 3 顺序、按真实游玩日期去重的连续天数；保留旧 best，不迁移错误 streak。v2 固定种子练习牌局附规则版本与指纹。全年 365 日期布局去重、两种模式共 730 个合法通关验证；不是随机均匀 Klondike 发牌，也不是用户留存证明。
- B02/B05：叠塔保留双轴玩法，碰撞使用真实矩形交集，画面显示 X/Z 与深度；同一投影绘制目标和落块，新层在旧层上方；物理时间步和真实比赛时钟分离，明确暂停时间不计入冲刺。
- B03：两条游戏详情 GET 在本地模式不启动远程查询，覆盖远程、超时、下架与无配置条件。
- O02：公共 locale 根与后台文档根独立，服务器按路由参数产生语言；构建产物只校验不改写。ISR 门禁在临时副本让缓存过期，验证 STALE → 重新生成 → HIT，并检查原始 HTML。
- O03：启动尝试、iframe load 观察、受控运行时握手、首次有效输入分层。消息检查 source/origin/slug/会话随机值并去重；第三方未知可玩状态不伪装成功。详见埋点口径变更说明。
- O04：新增和变更来源需要完整的独立许可与可玩性审核记录；旧目录准确来源冻结为兼容例外，不能通过沿用旧 slug 换源。发布服务写路径同样受门禁约束。保留已有 noindex 和人工审核限制。

## 验收方式

本地已执行 lint、type-check、internal-links、source-evidence、完整 Vitest、生产构建及 ISR 校验。完整单测 106 个文件 / 378 项通过；构建生成 159 个静态页面，50 个英文和 50 个中文静态 HTML 无须修补；ISR 六条不同类型中英文路径均完成重新生成验证。

新增浏览器回归含两种纸牌模式完整通关、冻结时钟、撤销/重玩及去重；禁用存储；叠塔双轴、键盘重复、长帧与暂停；运行时假消息和首次有效输入；无 JavaScript 语言与实验页 noindex。

本地 Chromium 与 Android 仿真及完整 GitHub 多浏览器 CI 结果以本次分支最终提交对应的验收记录 / PR Checks 为准，不将旧 main 的 CI 结果当作新补丁验证。不存在独立人工/外部审查通过的声明。

## 外部证据缺口和发布边界

200 条存量目录来源的独立商业授权证据仍未获取到。逐条清单保存在 docs/licenses/2026-09-05-来源证据缺口清单.json；存量例外不是许可证明，不代表侵权结论；不能仅以可访问或 noindex 替代许可。

未获取生产数据库设置、GA4 真实会话、各版权方授权及生产真机验证。没有合并 main，没有主动部署生产，没有更改账号凭证、付款、生产数据库或搜索引擎索引状态。

回滚应回退本次整改提交并重新执行门禁，不应强推或重写历史。旧 best 存储格式保留；新全局日期记录采用独立 v1 key，不删除原始用户记录。
