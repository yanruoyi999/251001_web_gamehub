# Clean branch note

This branch is based on latest `main` and intentionally excludes the older PR #4 monetization/API fallback changes, because `main` has advanced substantially since that PR was created.
